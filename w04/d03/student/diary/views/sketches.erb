<h1>All the Sketches!</h1>

<ul>
  <% @sketches.each do |sketch| %>
    <li>Sketch from <%= sketch["sketch_date"] %>
      <img src="<%= sketch["sketch_url"] %>" alt=""></li>
  <% end %>
</ul>

#note this code doesn't work- look it up in the instructor file
